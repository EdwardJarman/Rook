{
  "name": "rook-cli",
  "version": "0.1.0",
  "private": true,
  "description": "Rook in your terminal: same models as the web app, one-script install.",
  "type": "module",
  "bin": {
    "rook": "dist/rook.cjs"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "superjson": "^1.13.3"
  },
  "devDependencies": {
    "@types/node": "^22.19.3",
    "esbuild": "^0.25.12",
    "typescript": "~5.9.3",
    "vitest": "^2.1.9"
  }
}
